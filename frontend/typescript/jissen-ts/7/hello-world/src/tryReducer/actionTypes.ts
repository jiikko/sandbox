/*
export = {
  INCREMENT: "PREFIX_INCREMENT",
  DECREMENT: "PREFIX_DECREMENT",
  SET_COUNT: "COUNT"
} as const
*/
export const INCREMENT = "PREFIX_INCREMENT" as const
export const DECREMENT = "PREFIX_DECREMENT" as const
export const SET_COUNT = "COUNT" as const
